import 'package:flutter/material.dart';

class EncuestaBase extends StatelessWidget {
  const EncuestaBase({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Hola mundo")),
    );
  }
}